# CODEX PROMPT: Имплементация Institutional Exploration Module (IEM)

## Контекст для CODEX
Ты выступаешь в роли Senior Backend/Fullstack Engineer (CODEX) в рамках Antigravity Software Factory.
**Твоя текущая глобальная задача:** Полная end-to-end имплементация макро-модуля "ИССЛЕДОВАНИЯ" (Institutional Exploration Module).

Этот модуль является "Лабораторией" компании — изолированной R&D песочницей (Dual Innovation Architecture), которая собирает рыночные боли, внутренние идеи и алерты ИИ, фильтрует их через строгий процесс триажа и переводит в архитектурные спецификации или новые продукты без засорения операционного контура.

**Главный архитектурный документ:** `f:\RAI_EP\docs\10_FRONTEND_MENU_IMPLEMENTATION\19_MENU_Исследования.md`. 
Ты обязан изучить его от корки до корки перед написанием первой строчки кода.

---

## 🛑 Фундаментальные Инварианты (Strict Directives)

1. **Zero Trust Tenant Isolation:** В КАЖДОЙ новой модели Prisma обязано присутствовать поле `companyId: String` и индекс по нему. Ни один эндпоинт не имеет права отдавать или мутировать данные без фильтрации по `companyId` из контекста авторизации.
2. **Immutable Governance (Append-Only):** Любые голосования/решения в рамках *War Room* пишутся **только** через инсерты в таблицу событий (`WarRoomDecisionEvent`) с хешированием. Удаление исторических данных **категорически запрещено**.
3. **Canonical FSM Definition:** Жизненный цикл строго управляется следующим графом. Любые другие стейты или переходы запрещены.
   - **Стейты:** `DRAFT`, `IN_TRIAGE`, `BOARD_REVIEW`, `ACTIVE_EXPLORATION`, `WAR_ROOM`, `IMPLEMENTED`, `POST_AUDIT`, `REJECTED`, `ARCHIVED`
   - **Allowed Transitions:** 
     `DRAFT -> IN_TRIAGE`
     `IN_TRIAGE -> BOARD_REVIEW`
     `BOARD_REVIEW -> ACTIVE_EXPLORATION`
     `ACTIVE_EXPLORATION -> WAR_ROOM` (опционально)
     `WAR_ROOM -> ACTIVE_EXPLORATION`
     `ACTIVE_EXPLORATION -> IMPLEMENTED`
     `IMPLEMENTED -> POST_AUDIT`
     `ANY -> REJECTED`
     `POST_AUDIT -> ARCHIVED`
   - **Forbidden:** `ACTIVE_EXPLORATION -> DRAFT`, `BOARD_REVIEW -> DRAFT`, `IMPLEMENTED -> ACTIVE_EXPLORATION`.
4. **Governance Authority Matrix:** Ролевая модель (RBAC) должна строго соблюдаться на уровне сервисов (не только UI):
   - `Initiator`: Can Triage (No), Can Escalate (No), Can Approve (No), Can Open WarRoom (No)
   - `Triage Officer`: Can Triage (Yes), Can Escalate (Yes), Can Approve (No), Can Open WarRoom (No)
   - `SEU Board`: Can Triage (No), Can Escalate (No), Can Approve (Yes), Can Open WarRoom (Yes)
   - `Solver`: Can Triage (No), Can Escalate (No), Can Approve (No), Can Open WarRoom (Yes)
5. **Risk Integration Directive:** Каждый `ExplorationCase` обязан включать класификацию риска (интеграция с RiskGate):
   - `riskLevel`: ENUM (`R1`, `R2`, `R3`, `R4`)
   - `budgetImpact`: Decimal
   - `requiresBoardReview`: Boolean (computed на базе риска и бюджета)
6. **Ledger Integrity Contract (Экономика):** Экономические оценки (`expectedROI`, `actualROI`) имеют тип `Decimal`. Поле `evidenceRefs` (доказательная база) **обязано ссылаться на `LedgerEntry.id`**. Транзакция должна быть провалидирована (verification confirmation) до перевода кейса в статус `POST_AUDIT`. Economic lock обязателен до верификации.
7. **Read Model Enforcement (UI Policy):** Frontend категорически не имеет права вычислять стейты или дедуцировать бизнес-логику. UI потребляет **только read-model эндпоинты**. Вся валидация переходов (disable rules), логика optimistic updates и права доступа (canTransition) отдаются строго через API.

---

## 📋 Поэтапный план реализации (Execution Checklist)

Твоя задача — реализовать модуль поэтапно. Не перескакивай через фазы. После завершения каждой фазы ты обязан обновить этот чеклист и сделать коммит.

### Phase 1: Data Architecture & Prisma Schema
Твоя первая цель — спроектировать структуры данных в `schema.prisma`.
Опирайся на раздел 12 документа `19_MENU_Исследования.md`.

- [ ] Изучить текущий `schema.prisma` для понимания стилистики и текущих моделей.
- [ ] Добавить `Enum` типы (SignalSource, SignalStatus, ExplorationType, ExplorationMode, TriageStatus, WarRoomStatus, RoiSource и т.д.).
- [ ] Создать модель `StrategicSignal` (Signal Layer) с полем `duplicateOf` (self-relation) и `initiatorId`.
- [ ] Создать базовую модель `ExplorationCase` (Deep Work Layer) с жёсткой привязкой к `StrategicSignal`.
- [ ] Реализовать модели для Governance & War Room: `WarRoomSession` и иммутабельный лог `WarRoomDecisionEvent`.
- [ ] Реализовать модели экономики и трекинга: `ImpactAuditRecord` и `RewardRecord`.
- [ ] Добавить конфигурационные таблицы: `LabCapacityConfig`, `ExternalSourceAllowlist`, `AIScanRunLog`.
- [ ] Сгенерировать Prisma Client (`npx prisma generate`).

### Phase 2: Core Services & FSM (Backend)
Реализация бизнес-логики и защищенных сервисов.

- [ ] Создать `SignalTriageService`:
  - `ingestSignal` (создание `StrategicSignal`).
  - `clusterDuplicateSignals` (простановка ссылок дублей).
  - `triageToCase` (трансформация сигнала в `ExplorationCase` со статусом `DRAFT`).
- [ ] Создать `ExplorationGovernanceService` (FSM для кейсов):
  - Метод перехода `DRAFT -> IN_TRIAGE`.
  - Метод эскалации `IN_TRIAGE -> BOARD_REVIEW` (обязателен для SEU).
  - Метод старта `BOARD_REVIEW -> ACTIVE_EXPLORATION` (с проверкой budget lock).
- [ ] Создать `WarRoomService`:
  - Инициализация сессии `WarRoomSession`.
  - Регистрация иммутабельного события `WarRoomDecisionEvent`.
  - Авто-расчёт `resolutionLog` на основе событий.
- [ ] Покрыть критические переходы FSM Unit-тестами (Zero Trust Checks).

### Phase 3: API Layer (Controllers & Routes)
Публикация эндпоинтов для фронтенда в соответствии с контрактами.

- [ ] Настроить маршруты в контроллере (например, `ExplorationController`).
- [ ] `POST /api/exploration/signals` (Intake).
- [ ] `POST /api/exploration/cases/from-signal/:signalId` (Triage).
- [ ] `GET /api/exploration/showcase` (Получение витрины прототипов, с пагинацией и фильтрами).
- [ ] Настроить защиту эндпоинтов (проверка JWT/Session, Middleware проверки tenant `companyId`).
- [ ] Проверить Swagger/OpenAPI спецификацию для новых роутов.

### Phase 4: Frontend UI Framework
Интеграция в существующий UI-скелет (React/Next.js).

- [ ] Обновить навигацию (сайдбар) — добавить пункт `"ИССЛЕДОВАНИЯ"`.
- [ ] Создать корневой layout `/exploration/layout.tsx`.
- [ ] Реализовать страницу `/exploration/page.tsx` (Dashboard "Витрина прототипов").
- [ ] Создать страницы `/exploration/strategic` (SEU Panel) и `/exploration/constraints` (CDU Panel).
- [ ] Реализовать компонент `TriageInputForm` для подачи новых сигналов.
- [ ] Связать UI компоненты с API (настроить React Query / SWR / Fetch-хуки).
- [ ] Проверить соблюдение UI-канона (`rounded-2xl`, светлая тема, состояния загрузки/ошибки).

### Phase 5: Verification & E2E
Доказательство стабильности модуля.

- [ ] Написать E2E тест на полный цикл: *Создание Strategic Signal -> Триаж в Exploration Case -> Переход в War Room -> Запись события решения -> Переход в IMPLEMENTED -> Вызов Post Audit*.
- [ ] Намеренно вызвать ошибку прав доступа (проверить Zero Trust Isolation) в Unit-тестах.
- [ ] Обновить `99_TECH_DEBT_CHECKLIST.md` (вычеркнуть `TD-EXP-UI-001` и `TD-EXP-API-001`).

---

## 🧠 Инструкция по выполнению
1. Начинай строго с **Phase 1**. Сначала схема базы данных, затем логика, затем API, затем UI.
2. Никаких домыслов. Если в `19_MENU_Исследования.md` поле называется `explorationMode` с типом ENUM (SEU, CDU), в коде должно быть в точности так же.
3. Архитектура должна быть Institutional-Ready. Каждая написанная функция должна подразумевать, что завтра в эту систему придут аудиторы из Big4.
4. Отчитывайся о завершении каждой фазы (или значимого куска) и помечай чекбоксы как выполненные `[x]`.
