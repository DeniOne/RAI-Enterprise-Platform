# RAI_EP — Реальная карта проекта (контракт → факт)  
Дата: 2026-03-01  
Статус: рабочий документ для планирования следующего “тонкого среза”

## Легенда статусов
- **DONE**: есть в коде и похоже подключено к runtime.
- **PARTIAL**: часть сделана, но не замкнуто end-to-end или не подключено.
- **MOCK**: UI/endpoint есть, но логика заглушка.
- **SPEC-ONLY**: описано/лежит в `docs/`, но в `apps/*` как модуль не найдено.
- **UNKNOWN**: без запуска/проверки нельзя честно подтвердить (нужна верификация).

## Сводная таблица (самое важное)

| Поток/модуль | Контракт (что “должно быть”) | Факт в коде (что реально есть) | Доказательство (ключевые файлы) | Статус | Дыра/риск (что сломает задумку) | Следующий самый короткий шаг |
|---|---|---|---|---|---|---|
| Agent-First LAW (TechMap в центре) | TechMap — главный артефакт; Telegram intake; Draft→Commit; Controller; widgets. | TechMap и бизнес-модули есть; Agent OS как единый контур пока не собран. | `docs/00_STRATEGY/STAGE 2/SPEC_AGENT_FIRST_RAI_EP.md` | PARTIAL | Закон есть, принуждения end-to-end по агентному потоку нет. | Выбрать 1 тонкий срез и закрыть его “без обходов” (draft→must→confirm→commit). |
| Web Agent OS (Shell: чат слева + контекст страницы + виджеты справа) | Постоянный чат в Shell; `WorkspaceContext`; `/api/rai/chat`; `widgets[]`; MemoryAdapter. | Постоянная точка входа чата в UI есть, контекст по route прокидывается; Реализован канонический эндпоинт `POST /api/rai/chat` в `apps/api` и подключен в `AiChatStore`. | `apps/web/app/layout.tsx`; `apps/web/components/ai-chat/AiChatRoot.tsx`; `apps/web/app/api/ai-chat/route.ts`; `docs/00_STRATEGY/STAGE 2/RAI_AGENT_OS_IMPLEMENTATION_PLAN.md` | DONE | Если продолжать пилить UI без настоящего агентного API, получится “чатик с анимацией”, не OS. | Перенести/добавить реальный чат endpoint в `apps/api` (по контракту) и подключить web к нему (без мока). |
| WorkspaceContext (как агент “видит” страницу) | Контракт `WorkspaceContext`: route + активные refs + краткие summary, без тяжёлых данных. | В веб-чате сейчас обновляется только `route`; канонического `WorkspaceContext` store по страницам не видно. | `apps/web/components/ai-chat/AiChatRoot.tsx`; `docs/00_STRATEGY/STAGE 2/RAI_AGENT_OS_IMPLEMENTATION_PLAN.md` | PARTIAL | Агент не сможет делать “реактивные” ответы/виджеты по сущностям, если контекст — только url. | Ввести единый `WorkspaceContext` и публикацию refs на ключевых страницах (CRM/TechMap/Operations). |
| Typed tool calls (запрет string-execution) | Агент вызывает домен только типизированными вызовами (`draftCreate/validate/commit/...`). | В web-чате `suggestedActions: any[]` (не типизировано, не исполняется). Контрактный подход есть в других модулях, но не в Agent OS. | `docs/00_STRATEGY/STAGE 2/SPEC_AGENT_FIRST_RAI_EP.md`; `apps/web/app/api/ai-chat/route.ts` | PARTIAL | Без реестра инструментов и валидации “typed tools” останутся лозунгом. | Завести серверный registry инструментов + строгие схемы payload’ов + аудит вызовов. |
| Tenant isolation 10/10 | Все tenant-scoped данные изолируются по `companyId` из доверенного контекста; нельзя принимать `companyId` из payload. | В `apps/api` реально внедрён proxy `$extends` и инъекция `companyId` на уровне Prisma. | `docs/01_ARCHITECTURE/PRINCIPLES/SECURITY_CANON.md`; `apps/api/src/shared/prisma/prisma.service.ts` | DONE | Риск не в механике, а в “обходных” контроллерах/моках, которые подсовывают `companyId` руками. | Дальше держать дисциплину: любые новые API должны брать `companyId` только из контекста. |
| Unified Memory (Redis + pgvector) | Working (Redis) + Episodic (pgvector); retrieval по политике; объяснимость/уверенность. | Модуль памяти в `apps/api` есть (`MemoryManager`, `EpisodicRetrievalService`, `ShadowAdvisoryService`). `MemoryEntry` в Prisma есть. | `apps/api/src/shared/memory/memory-manager.service.ts`; `apps/api/src/shared/memory/episodic-retrieval.service.ts`; `packages/prisma-client/schema.prisma` (model `MemoryEntry`); `docs/01_ARCHITECTURE/HLD/UNIFIED_MEMORY_ARCHITECTURE.md` | PARTIAL | Память существует как инфраструктура, но она пока не “память чата агента”, потому что сам web-чат — мок и не ходит в `apps/api`. | Подключить web-чат к `apps/api` и встроить retrieve/append в чат-пайплайн. |
| Advisory (shadow) и объяснимость | Рекомендации с explainability и confidence; human-in-the-loop. | В `apps/api` есть advisory модуль и тесты; есть shadow advisory и episodic retrieval. | `apps/api/src/modules/advisory/*`; `apps/api/src/shared/memory/shadow-advisory.service.ts`; `docs/01_ARCHITECTURE/PRINCIPLES/SECURITY_CANON.md` | DONE/PARTIAL | Работает как отдельная вертикаль, но не замкнуто с Agent OS UX (виджеты/чат). | Свести выдачу advisory в общий агентный протокол (widgets справа + chat слева). |
| TechMap (ATK / plan-fact эталон) | Детальная карта — контрактный эталон; план/факт → отклонения → решения. | Есть `TechMap` модуль в `apps/api` и consulting-валидация; в доках описан детальный контракт. | `apps/api/src/modules/tech-map/*`; `apps/api/src/modules/consulting/tech-map.*`; `docs/02_DOMAINS/AGRO_DOMAIN/TECH_MAP_DETAILED.md` | DONE/PARTIAL | Если события поля (Telegram intake) не коммитятся корректно, план/факт и отклонения будут “без факта”. | Замкнуть событийный контур: committed events → controller → deviations → widgets/alerts. |
| Agro Telegram Intake: Draft→Fix/Link→Confirm→Commit | Telegram input → `EventDraft` → MUST-вопросы → ✅ confirm → commit event + evidence + provenance. | В `packages/prisma-client/schema.prisma` модели `AgroEventDraft/Committed` есть, но реализация intake/actions найдена только в `docs/02_DOMAINS/...` (как “код-спека”), в `apps/api/src` модулей не найдено. | `packages/prisma-client/schema.prisma` (AgroEventDraft/Committed); `docs/07_EXECUTION/AGRO_TELEGRAM_INTAKE_CHECKLIST.md`; `docs/02_DOMAINS/AGRO_DOMAIN/EVENTS/*` | SPEC-ONLY (по текущему поиску) | Это критический разрыв: без реального Draft→Commit Telegram остаётся транспортом/моком, а не “терминалом поля”. | Перенести этот контур из `docs/` в реальный модуль `apps/api` и подключить бота к нему. |
| Agro Controller + escalations | plan/fact → deviation severity S0..S4 → эскалации S3/S4. | В Prisma модель `AgroEscalation` есть; но в `apps/api/src` использование/сервисы по `AgroEscalation` поиском не подтверждены (нужна верификация). | `packages/prisma-client/schema.prisma` (model `AgroEscalation`); `memory-bank/activeContext.md` (заявлено “сделано”) | UNKNOWN | Если эскалации заявлены как “готово”, но реально не подключены — ты будешь принимать решения по фантомам. | Верифицировать: найти сервис/контроллер в `apps/api`, который пишет `AgroEscalation`, и тест, который это доказывает. |
| CRM (контрагенты/хозяйства) | Регистрация сущностей, юр. реквизиты, автозаполнение (DaData), роли. | CRM модуль в `apps/api` есть; commerce/party тоже есть (не одно и то же). | `apps/api/src/modules/crm/*`; `apps/api/src/modules/commerce/*`; `memory-bank/task.md` (party lookup) | DONE/PARTIAL | Риск в раздвоении “CRM vs Commerce Party”: можно сломать единую модель контрагентов. | Зафиксировать границы: что является каноническим “контрагентом” для каких потоков. |
| Telegram transport (бот) | Telegram — основной “field terminal”, минимум кнопок ✅✏️🔗. | Есть отдельный `apps/telegram-bot` (транспорт) и отдельный `mg-core/backend/src/mg-chat` (контрактный телеграм-чат). | `apps/telegram-bot/*`; `mg-core/backend/src/mg-chat/*`; `telegram/README.md` | PARTIAL | Два параллельных телеграм-контура = риск раздвоения “единственного терминала поля”. | Выбрать канонический телеграм-контур и привязать его к Draft→Commit. |

## Самое важное (в одном абзаце)
У тебя сильная платформа (tenant isolation, Prisma, модули TechMap/CRM/Advisory), но Agent OS “как задумано” сейчас ломается в двух местах: **web-чат — мок** и **agro Telegram Draft→Commit выглядит как SPEC-ONLY (в `docs/`)**. Пока эти две штуки не станут боевыми, любые дальнейшие планы про “виджеты, память, реактивность” будут косметикой.

