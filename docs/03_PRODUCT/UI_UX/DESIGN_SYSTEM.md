---
id: DOC-PRD-GEN-097
type: UI Spec
layer: Product
status: Approved
version: 1.1.0
owners: [@techlead, @design-team]
last_updated: 2026-02-20
---

# UI Design Canon: RAI Enterprise Platform

> **Scope:** Enterprise (Contour 1) & Field (Contour 2)
> **Base Style:** MatrixGin Light (Geist Canon)

## 1. Концепция: Два Интерфейса

Система спроектирована с учетом радикально разных физических сред использования. Мы не делаем «универсальный адаптив», мы создаем два целевых контура.

### 🏢 Contour 1: Enterprise Web (Admin/Office)
* **Целевая аудитория:** CEO, HR, Бухгалтер, Главный агроном, Risk Officer.
* **Среда:** Desktop (23" - 27" Monitors), Laptop. Хорошее освещение, точная мышь.
* **Стиль:** **Information Dense**. Высокая плотность данных. Много таблиц, дашбордов, мелких контролов (data windowing).
* **Theme:** Light Mode Only (Professional SaaS). Строгое разделение фона и рабочих поверхностей для предотвращения усталости глаз.

### 🚜 Contour 2: Field Mobile (Agro/Machinery)
* **Целевая аудитория:** Агроном-консультант, Механизатор, Скаут.
* **Среда:** Планшет в кабине трактора, Телефон в поле (яркое солнце, ночь, вибрации, работа в перчатках).
* **Стиль:** **Touch First**. Огромные мишени для клика, жесткая фильтрация выводимой информации.
* **Theme:** Auto (Light for Day, High-Contrast Dark for Night). 

---

## 2. Typography (Geist Canon)
**Font:** `Geist Sans` (UI) / `Geist Mono` (Data). No fallbacks.

| Element | Weight | Utility | Note |
| :--- | :--- | :--- | :--- |
| **Headers** | 500 | `font-medium` | Без Bold. Акцент достигается управлением размером (`text-xl`, `text-2xl`), сохраняя чистоту. |
| **Body (Desktop)** | 400 | `text-sm` | 14px. Оптимально для плотных интерфейсов (Contour 1). |
| **Body (Mobile)** | 500 | `text-base` | 16px+. Повышенная плотность и размер компенсируют тряску и солнце (Contour 2). |
| **Numbers & Data** | 500 | `font-mono` | Табличные данные, урожайность, финансы. Обеспечивает идеально ровные колонки. |

---

## 3. Color Palette & Semantics

### Base (Foundation & Elevation)
Для предотвращения эффекта «снежной слепоты» на больших экранах используется концепция пространственной глубины (Холст и Поверхности).

* **Global Canvas (Фон приложения):** `bg-slate-50` (`#F8FAFC`). Снимает зрительное напряжение.
* **Surfaces (Рабочие панели, Карточки):** `bg-white` (`#FFFFFF`) с границей `border border-slate-200` или легкой тенью `shadow-sm`.
* **Inputs / Forms:** `bg-slate-50/50` для легкого выделения на фоне белой карточки.
* **Ink (Text):** `#030213` (Смягченный черный — снижает контрастную усталость глаз).
* **Muted (Text):** `#717182` (Вторичная информация, плейсхолдеры).

### High-Contrast Dark Mode (Contour 2 Only)
Для работы в кабине техники ночью (Contour 2) темная тема — это вопрос безопасности (предотвращение ослепления).

* **Surface:** `bg-[#0B0C10]` (Глубокий сине-черный).
* **Elevated Surface (Cards):** `bg-[#1A1C23]`.
* **Primary Text:** `text-white/90` (с легким `opacity-90` для снижения «свечения» букв).
* **Muted Text:** `text-[#94A3B8]` (Slate-400).
* **High Contrast Action:** Акцентные цвета (`Emerald-500`, `Indigo-400`) должны иметь коэффициент контрастности не менее 7:1 относительно фона (WCAG AAA).
* **Visual Aid:** Все иконки в темной теме получают микро-свечение (shadow-glow) соответствующего семантического цвета для мгновенного распознавания при тряске.

### Branding (Contour Specific)
* 🏢 **Enterprise Primary:** Indigo-600 (`#4F46E5`). Транслирует строгость, технологичность и безопасность.
* 🚜 **Field Action:** Emerald-600 (`#059669`). Четкий сигнал к действию («Запустить», «Подтвердить факт»).

### Semantic States (FSM & System Events)
Цвета строго привязаны к бизнес-логике и состояниям Ledger:
* 🔴 **Error / Critical (`Red-600`):** Ledger mismatch, отказ в M-of-N подписи, критическое падение метрик.
* 🟡 **Warning / Risk (`Amber-500`):** Вероятностное отклонение плана от факта, риски, ожидание консенсуса.
* 🟢 **Success / Verified (`Emerald-500`):** Данные закреплены в Ledger, транзакция исполнена, факт подтвержден.
* 🔵 **Info / Processing (`Blue-500`):** Системные уведомления, работа AI-агента (генерация).

---

## 4. Spacing System (8px Grid)

Интерфейс строится на базовом модуле 8px (и его половинке 4px для микро-отступов). 

* **Contour 1 (Enterprise):** * Внутренние отступы компонентов: `4px` (`p-1`), `8px` (`p-2`). 
    * Расстояние между блоками: `16px` (`gap-4`).
* **Contour 2 (Field):**
    * Внутренние отступы компонентов: минимум `12px` (`p-3`) или `16px` (`p-4`).
    * Расстояние между блоками: `24px` (`gap-6`).

---

## 5. Interaction & Ergonomics (Touch Targets)

Правила физического взаимодействия для предотвращения ошибок ввода:

* **Contour 1 (Mouse):** Минимальный размер кликабельной зоны — `32x32px` (стандартный инпут, иконка-кнопка).
* **Contour 2 (Touch / Gloves):**
    * Минимальный размер мишени: **`48x48px`**.
    * Рекомендованный размер для главных кнопок (Call to Action): **`56x56px`** или `64x64px`.
    * Расстояние между соседними интерактивными элементами — не менее `8px` для исключения мисскликов при тряске.

---

## 6. Complex UI States (Edge Cases)

### 📶 Offline & Sync Mode (Contour 2)
Агроном в поле регулярно работает без связи. Интерфейс должен это четко, но без паники отражать.
* **Индикация:** Серый бейдж в шапке `[☁️ Оффлайн. Последняя синхронизация: 12:04]`.
* **Взаимодействие:** Кнопки сохранения меняются на `Сохранить локально` (с иконкой дискеты/устройства). 
* **Восстановление:** При появлении сети бейдж меняется на синий спиннер `[🔄 Синхронизация...]`, затем скрывается.

### ⚔️ Conflict State (Level C - Deviations)
Когда система выявляет расхождение (План vs Факт) или конфликт консенсуса.
* **Визуализация:** Блок с конфликтными данными подсвечивается бордером `border-red-500` и легким красным фоном (или `bg-red-900/20` для Dark Mode).
* **Разрешение (Resolution Panel):** Появление обязательного текстового поля `Justification` (Обоснование). Без заполнения этого поля кнопка "Переопределить (Override)" заблокирована.

### 🔐 Immutable & Signature States (Level F)
Отображение криптографических и Ledger-зависимостей.
* **Immutable Lock:** Рядом с полями, которые уже зафиксированы в блокчейне (например, закрытая техкарта), выводится иконка замка (`🔒`). Инпуты получают `opacity-60` и `cursor-not-allowed`.
* **Signature Pending:** Блок или кнопка покрывается полупрозрачным оверлеем (blur). Выводится прогресс-бар: `Ожидание подписи (1/3) [Главный Агроном: 🟢] [СЕО: ⚪] [CFO: ⚪]`.

---

## 7. Governance & Authority Patterns

Для обеспечения институциональной надежности UI не должен принимать решения на основе «роли» пользователя. Все критические действия управляются через контекст полномочий (**AuthorityContext**).

### 7.1. Action Gating Pattern
Каждый компонент принятия решений (Decision Component) получает контекст:

```typescript
interface AuthorityContext {
  canOverride: boolean;  // Право нарушить инвариант (Level C)
  canSign: boolean;      // Право подписи в M-of-N (Level F)
  canEscalate: boolean;  // Право отправить на уровень выше
  canEdit: boolean;      // Право менять данные в текущем состоянии FSM
}
```

**Правило:** UI-логика запрашивает флаги полномочий у `GovernanceService`. Рендеринг кнопок и инпутов зависит только от этих флагов.

### 7.2. Governance-Aware Components
Управляющие кнопки (`Override`, `Sign`, `Approve`, `Freeze`, `Publish`) должны следовать протоколу:

1.  **Strict Gating:** Если действие запрещено — кнопка либо скрывается (если пользователь не должен знать о возможности), либо отображается в `disabled` состоянии.
2.  **Reason Tooltip:** Любой `disabled` контрол управления обязан иметь тултип с технической причиной блокировки (напр. *"Недостаточно веса подписи для текущего лимита"*).
3.  **Fallback to Escalate:** Если `canApprove === false`, но есть риск, кнопка "Approve" может автоматически заменяться на "Escalate to Superior" (Эскалировать запрос).

---

## 8. Escalatory Governance Protocol (Non-Blocking Model)

Система RAI придерживается принципа «мягкого контроля»: вместо жестких блокировок мы используем процедурную эскалацию, что позволяет не останавливать бизнес-процессы, сохраняя при этом юридическую чистоту и подотчетность.

### 8.1. Decision Flow
Любое действие пользователя проходит через цикл:
1.  **User Action Initiated** (Нажатие кнопки).
2.  **Integrity Check** (Проверка инвариантов сервисом `IntegrityGate`).
3.  **Bifurcation:**
    *   *If Normal:* Действие подтверждается (Proceed).
    *   *If Deviation:* Триггерится автоматическая эскалация (Escalation Triggered).
4.  **Committee Review:** Задача попадает в очередь профильного комитета (Risk/Strategic/Finance).
5.  **Resolution:** Фиксация окончательного решения (Approve/Reject) с записью в Ledger.

### 8.2. UI behavior при инцидентах R3 / R4
Если действие вызывает серьезное отклонение (уровни R3 или R4 по матрице рисков), интерфейс не блокирует пользователя, а переходит в **Escalation Awareness Mode**:

*   **Visual Alert:** Поверх формы или рядом с кнопкой появляется красный баннер (`bg-red-600` в Dark Mode / `bg-red-50`).
*   **Warning Text:** *"Внимание: Данное действие инициирует немедленное рассмотрение ТехСоветом"*.
*   **Contextual Data:** В баннере выводятся обязательные метаданные:
    *   `Escalation ID` (Уникальный номер тикета).
    *   `Target Committee` (Комитет-получатель).
    *   `Expected Quorum` (Необходимое кол-во подписей, напр. 2/3).
    *   `Trace ID` (Трассировка события).
*   **Button State:** Кнопка не исчезает и не становится `disabled`. Она получает статус **Escalatory Action**, визуально выделяясь (например, двойная рамка или спец-иконка эскалации). При нажатии пользователь подтверждает осознанный вход в процедуру эскалации.

### 8.3. Override UX
**Override ≠ обход системы.**

В нашей дизайн-системе `Override` — это не «чит-код» для игнорирования правил, а **инициирование процедуры повышенного уровня контроля**.

*   **Visual Philosophy:** Кнопка `Override` должна выглядеть «тяжелой» и требовать подтверждения (Confirmation Step).
*   **Semantic Shift:** Вместо *"Я хочу игнорировать ошибку"*, интерфейс транслирует *"Я беру личную ответственность и передаю вопрос на уровень выше"*.
*   **Requirement:** Любой `Override` визуально подчеркивается в Ledger-логах и аудит-трейлах как событие с высоким приоритетом.
