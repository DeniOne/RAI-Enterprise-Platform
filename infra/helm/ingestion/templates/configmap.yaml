apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-ingestion-config
  namespace: {{ .Values.namespace }}
  labels:
    app: {{ .Release.Name }}
    component: ingestion
    version: {{ .Values.serviceVersion | quote }}
data:
  SERVICE_VERSION: {{ .Values.serviceVersion | quote }}
  # Sprint 1 scaffolding only. Реальная конфигурация добавится в Sprint 2+.
