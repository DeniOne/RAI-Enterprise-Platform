// AUTO-GENERATED by scripts/split-prisma-schema-by-domain.cjs
// Fragment: 00_base.prisma

generator client {
  provider        = "prisma-client-js"
  output          = "./generated-client"
  previewFeatures = ["postgresqlExtensions"]
}

datasource db {
  provider   = "postgresql"
  url        = env("DATABASE_URL")
  extensions = [vector]
}

// --- Multi-tenancy Models ---

// Shared primitives only: generator/datasource + shared enums/models.

// Enums

enum MachineryType {
  TRACTOR
  SPRAYER
  HARVESTER
  ATTACHMENT
  TRUCK
}

enum TriggerOperator {
  GT
  GTE
  LT
  LTE
  EQ
  NOT_EQ
}

enum ClientType {
  FARMER
  DEALER
  HOLDING
  PARTNER
}

enum RiskLevel {
  LOW
  MEDIUM
  HIGH
  CRITICAL
}

enum BudgetStatus {
  DRAFT
  APPROVED
  LOCKED
  ACTIVE
  EXHAUSTED
  BLOCKED
  CLOSED
}

enum MeasurementSource {
  SENSOR
  MANUAL
  LAB
}

enum BudgetCategory {
  SEEDS
  FERTILIZER
  PESTICIDES
  FUEL
  LABOR
  MACHINERY
  RENT
  LOGISTICS
  ANALYSES
  OTHER
}

enum TriageStatus {
  PENDING
  ACCEPTED
  DUPLICATE
  REJECTED
}

