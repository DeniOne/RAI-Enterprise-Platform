{
  "name": "ingestion-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Scaffolding for ingestion worker (Sprint 1).",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "smoke": "tsc -p tsconfig.json && node dist/smoke.js"
  },
  "dependencies": {
    "ajv": "^8.12.0",
    "ajv-formats": "^2.1.1"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "@types/node": "^20.11.19"
  }
}
